<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:valueConverters="clr-namespace:FileConverter.ValueConverters"
             xmlns:project="clr-namespace:FileConverter.Properties"
             x:Class="FileConverter.Controls.EncodingQualitySliderControl"
             mc:Ignorable="d" d:DesignWidth="340" MinHeight="62">
    <UserControl.Resources>
        <valueConverters:BitrateToString x:Key="BitrateToString" />
    </UserControl.Resources>

    <Grid Margin="0,2,0,2">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="78" />
        </Grid.ColumnDefinitions>

        <Slider x:Name="slider" Grid.Column="0" Margin="0,4,10,0" VerticalAlignment="Center"
                AutoToolTipPlacement="TopLeft" IsSnapToTickEnabled="True" IsSelectionRangeEnabled="True"
                Minimum="65" Maximum="245" SelectionStart="115" SelectionEnd="245"
                SmallChange="8" LargeChange="32" TickPlacement="BottomRight" TickFrequency="8"
                Ticks="65 85 100 115 130 165 175 190 225 245"
                Foreground="{DynamicResource AccentBrush}" />

        <Border Grid.Row="0" Grid.Column="1" Background="{DynamicResource AccentSoftBrush}" CornerRadius="8" Padding="8,5">
            <TextBlock Text="{Binding Value, Converter={StaticResource BitrateToString}, ElementName=slider, Mode=OneWay}"
                       HorizontalAlignment="Center" Foreground="{DynamicResource MainDarkBrush}" FontWeight="SemiBold" />
        </Border>

        <StackPanel Grid.Column="0" Grid.Row="1" Orientation="Horizontal" Margin="2,4,0,0">
            <Border Width="8" Height="8" CornerRadius="4" Background="{DynamicResource AccentBrush}" Margin="0,2,6,0" />
            <TextBlock Text="{x:Static project:Resources.RecommendedBitrateRangeInBlue}"
                       Foreground="{DynamicResource TextMutedBrush}" FontSize="11" />
        </StackPanel>
    </Grid>
</UserControl>
